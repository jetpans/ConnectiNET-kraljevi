version: "3"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=<development or deployment>
      - SECRET_KEY=<Secret key>
      - DB_CONNECT_URL_DEV=<Development database URL>
      - DB_CONNECT_URL_PROD=<Deployment database URL>
